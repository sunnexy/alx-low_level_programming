#include "main.h"
/**
 * main - tests function that prints if integer is positive or negative
 * Description: tests function to print positive or negative integers
 * Return: always 0
 */
int main(void)
{
	int i;

	i = 98;

	positive_or_negative(i);
	return (0);
}
