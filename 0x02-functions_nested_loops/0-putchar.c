#include <stdio.h>
/**
 * main - entry point
 * Description: printing puchar
 * return: always 0
 */
int main (void)
{
	char a;

	for (a = 'a'; a <= 'z'; a++)
		putchar(a);
	for (a = 'A'; a <= 'A'; a++)
		putchat(a);
	putchar('\n');
	return (0);
}
